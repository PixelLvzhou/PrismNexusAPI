# 账户系统 - 验证检查清单

## 数据库配置
- [x] 数据库表已添加 `role` 字段（默认值 'user'）
- [x] 数据库表已添加 `avatar_url` 字段（默认值 ''）
- [x] 数据库表已添加 `bio` 字段（默认值 ''）
- [x] 数据库表已有 `status` 字段（默认值 'enabled'）
- [x] 现有用户数据已迁移，新增字段已填充默认值

## JWT认证中间件
- [x] 创建了 `middleware/auth.js` 文件
- [x] 中间件能正确解析JWT token
- [x] 无token请求返回401状态码
- [x] 无效token请求返回401状态码
- [x] 有效token请求能正确将用户信息附加到req对象

## 账户信息查询接口
- [x] 已注册 GET `/api/auth/me` 路由
- [x] 接口需要JWT认证
- [x] 接口返回用户信息包含：id, username, email, role, avatar_url, status, bio
- [x] 接口返回状态码200
- [x] 未登录状态调用返回401

## 注册接口改造
- [x] 注册新用户时插入新增字段的默认值
- [x] 默认权限等级为 'user'（普通用户）
- [x] 默认头像URL为空字符串
- [x] 默认账号状态为 'enabled'（正常）
- [x] 默认简介为空字符串
- [x] 注册成功返回状态码201

## 前端修改
- [x] NavbarRight组件在挂载时调用账户信息查询接口
- [x] 用户名称动态显示为接口返回的数据
- [x] 接口调用失败时有错误处理
- [x] 登录后首页右上角显示当前用户名称

## 整体功能验证
- [x] 新用户注册后能正确查询到完整信息
- [x] 现有用户登录后能正确查询到完整信息
- [x] 前端页面正确显示当前登录用户名称