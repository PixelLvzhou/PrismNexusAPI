# 权限管理系统 - 验收清单

## Phase 1: 后端基础建设

### 数据库变更
- [x] `permission_applications` 表创建完成，包含正确字段
- [x] `user_permissions` 表创建完成，包含正确字段
- [x] 数据库迁移脚本可正常执行

### API 接口
- [x] `POST /api/permission/apply` 接口返回正确
- [x] 申请时检查用户是否已拥有权限（已拥有返回提示）
- [x] 申请时检查是否有待审批申请（已有返回申请中提示）
- [x] 申请记录时效设置为24小时
- [x] `POST /api/permission/applications` 支持 query 类型
- [x] query 类型支持分页参数
- [x] query 类型支持状态筛选
- [x] `POST /api/permission/applications` 支持 cancel 类型
- [x] 取消申请只允许申请人本人操作
- [x] `POST /api/permission/applications` 支持 approve 类型
- [x] 审批通过后，用户权限表新增记录
- [x] `POST /api/permission/applications` 支持 reject 类型
- [x] 驳回操作支持填写原因
- [x] 待审批申请超过24小时自动标记为已过期
- [x] `GET /api/permission/my-permissions` 接口返回当前用户权限
- [x] `POST /api/auth/accounts` 接口返回 `permissions` 字段
- [x] `permissions` 字段格式为数组对象形式

### 权限控制
- [x] 只有开发者可以审批权限申请
- [x] 只有管理员可以提交权限申请
- [x] 普通用户不能访问权限相关接口

## Phase 2: 前端组件开发

### DetailDialog 组件
- [x] DetailDialog.vue 组件创建完成
- [x] 组件样式与 ConfirmDialog 保持一致
- [x] 组件保留插槽用于自定义内容
- [x] 支持关闭按钮
- [x] 支持点击遮罩关闭

### 权限管理页面
- [x] `/permission-manage` 路由配置正确
- [x] 路由守卫：仅管理员和开发者可访问
- [x] 页面包含开关切换（权限管理/权限申请）
- [x] 页面包含搜索框（支持按ID或用户名搜索）

### 权限管理列表容器
- [x] 列表展示字段完整（ID、头像、用户名、邮箱、角色、状态）
- [x] 权限能力列正确显示权限标签
- [x] 查看详情按钮可点击
- [x] 详情弹框正确显示权限列表详情

### 权限申请容器 - 仅开发者可见
- [x] 申请记录列表正确展示
- [x] 状态筛选功能正常（全部/待审批/已通过/已驳回/已过期）
- [x] 待审批记录显示通过/驳回按钮（仅开发者可见）
- [x] 驳回按钮弹出原因填写弹框
- [x] 已通过/已驳回/已过期记录仅展示
- [x] 此处无取消申请按钮（取消申请在导航管理页）

### 导航管理页权限申请功能
- [x] 权限申请按钮显示在搜索框右侧
- [x] 按钮状态根据用户权限状态正确显示
  - [x] 开发者：隐藏按钮
  - [x] 管理员+已有权限：禁用按钮，文案"已拥有"
  - [x] 管理员+无权限+无申请：可点击，文案"申请权限"
  - [x] 管理员+无权限+有申请中：禁用按钮，文案"已申请"（下方显示取消申请按钮）
  - [x] 管理员+无权限+已过期：可点击，文案"申请权限"
- [x] 点击申请按钮弹出二级确认弹框
- [x] 申请成功后页面状态正确更新

### 导航管理页申请记录展示 - 仅申请者本人可见
- [x] 申请记录正确展示在权限申请按钮下方
- [x] 展示内容完整（申请人、权限名称、申请时间、剩余时效、审批人、审批状态）
- [x] 状态颜色正确（待审批-黄、通过-绿、驳回-灰、过期-红）
- [x] 驳回状态显示驳回原因
- [x] 取消申请按钮仅在"有申请中"状态下显示
- [x] 取消申请需二级确认
- [x] 取消成功后按钮文案改为"已取消"并置灰

## Phase 3: 功能完善

### 前端API对接
- [x] permission API 模块创建完成
- [x] 权限申请接口调用正常
- [x] 申请列表查询接口调用正常
- [x] 审批/驳回/取消接口调用正常
- [x] 账户列表接口正确解析 permissions 字段

### 导航入口
- [x] 权限管理入口显示在导航管理下方
- [x] 入口**仅对开发者可见**（管理员不可见）

### 后端路由
- [x] 权限相关路由创建完成
- [x] 认证中间件正确应用
- [x] 权限控制正确执行

## 整体功能测试

### 权限申请流程
- [ ] 管理员提交权限申请成功
- [ ] 申请后按钮状态变为"已申请"
- [ ] 申请记录正确显示

### 权限审批流程
- [ ] 开发者可看到待审批申请
- [ ] 点击"通过"后，申请状态变为"已通过"
- [ ] 申请人获得相应权限

### 权限驳回流程
- [ ] 开发者点击"驳回"后弹出原因填写框
- [ ] 填写原因后提交，申请状态变为"已驳回"
- [ ] 申请人可看到驳回原因

### 取消申请流程
- [ ] 申请人点击"取消申请"后弹出确认框
- [ ] 确认取消后，申请状态变为"已取消"
- [ ] 按钮文案变为"已取消"并置灰

### 申请时效过期
- [ ] 超过24小时的待审批申请自动变为"已过期"
- [ ] 过期记录仍在列表中显示

### 账户权限状态展示
- [ ] 开发者查看权限管理页，可看到各账户的权限状态
- [ ] 权限列表正确显示各权限的拥有状态
